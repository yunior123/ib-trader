# AUDIT 2026-08-04 — lógica, calibración, arquitectura, realtime, logs

Auditoría profunda (TODO 10 + orden "hunt for logic issues, calibration issues, architecture
consistency, software realtime updates, broken things"). Todo hallazgo verificado contra el
fichero real; mediciones tomadas en vivo el 2026-08-04 17:20-18:00 ET (y re-medidas al cierre
del informe). NO se re-hallan los ya arreglados hoy: chain_loop del provider_bridge, paginación
Polygon, finviz 401, dedup de relés (f2274eab), notify_private.

## TOP-10 por severidad

| # | Fichero:línea | Qué pasa | Evidencia medida | Fix 1 línea |
|---|---|---|---|---|
| 1 | `scripts/provider_bridge.py:158-167` (productor) → `bots/*_signal_bot.cpp:176` (21 bots), `scripts/price_alarm.cpp:171`, `scripts/dip_alert.py:48` | El epoch de `data/nbbo_*.txt` es hora de BOLSA delayed 15 min, no wall-clock; los gates de ≤10 s / ≤180 s **no pueden cumplirse jamás** → los 21 bots bloquean TODA confirmación de compra (`SPREAD_MAX` armado, p.ej. `qqq_keepalive.sh:27 =0.3`; `qqq_signal_bot.cpp:1760-1766` con `sp=-1` fijo), price_alarm nunca usa NBBO, dip_alert **0 DIPs cantados** en todo el log | medido: `nbbo_spy` mtime_age=14 s pero epoch_age=**987 s**; `nbbo_qqq` 926 s; `grep -c DIP logs/dip_alert.log` → 0 | `provider_bridge.py:167`: escribir wall-clock 1º + hora-bolsa como 4º campo (`f"{int(time.time())} {bid:.4f} {ask:.4f} {ep}"`) — el `%lf %lf %lf` de los bots ignora el 4º sin romper |
| 2 | `scripts/bollinger_alarm.py:121` | `day0 = time.time() - time.time()%86400 + 9.5*3600` mezcla epoch UTC con offset local: la "sesión" arranca a las **05:30 EDT** → z-VWAP con 4 h de premarket contaminando el VETO (`ctx_ok`) que es gate literal de la voz (`:291,:294`) | medido en vivo: `time.strftime(...)` de esa expresión → `2026-08-04 05:30:00 EDT`; 185 barras premarket entran en la ventana | `day0 = time.mktime(time.localtime()[:3] + (9,30,0,0,0,-1))` |
| 3 | `scripts/direction_view.py:212` | `factors["fleet"]=f_fleet; weights["fleet"]=1.4` está FUERA del if/elif: cuando el capitán da 0 (incluye "faltan barras/excepción" en `signal_conditioning.py:157,171-172,175`) entra **0.0 con peso 1.4** → flecha diluida **28,6%** (peor que el precedente component_bias 18,5%). Consume `order_engine/prob_profit.py:181-186` (prob junto al botón BUY) | verificado: línea 212 sin sangría condicional; `fb["market"]=0` siempre que SPY y QQQ discrepan | `if f_fleet != 0.0: factors["fleet"]=f_fleet; weights["fleet"]=1.4` |
| 4 | `scripts/calibration_ledger.py:66-79` + `scripts/dailyplans_run.sh:25` | Loop de calibración empírica **CONGELADO desde el 22-jul**: `record_from_ranking` grepea `"ENTRADA:\s*reclaim de"` pero los x_drafts cambiaron a formato emoji (`▶️reclaim 305`) → 0 filas nuevas; `data/calibration.json` vive de UN día (21-jul, n=27) | `data/calib_log.jsonl`: 100% filas date=2026-07-21 (56); `logs/dailyplans.log`: "registradas 0 filas" ×6; autoimprove 2-ago: "calificadas 0 filas"; `grep -c ENTRADA x_drafts/*.txt` → 0 | actualizar el regex a `r"▶️reclaim ([\d.]+)"` (+targets/stops del formato emoji). Bonus: el ledger lee `~/Desktop/ib-trader/hoy` (`:254`) — TCC, mover a `~/ib-trader` |
| 5 | `scripts/fleet_consensus.py:41` (umbral 180 s) + `:214` (`sleep(45)` ×2 ciclos histéresis) | La alarma de MANADA estuvo **ciega el 99,6% de la mañana** (238/239 ciclos "cobertura insuficiente"): el productor escribía cada 243 s (mediana) hasta el fix de 15:17 vs umbral 180 s. Y el retraso estructural de la voz DANGER es 90 s (precedente literal "sleep(45)+histéresis") | `logs/fleet_consensus_py.log` 09:30-12:38: 238/239 ciegos; instancia pre-fix mediana mtime 243 s / p90 266 s; disparos MANADA hoy: 0 | `:41` `MAX_BAR_AGE=240`; `:214` `time.sleep(20)` (retraso 90→40 s) |
| 6 | `scripts/compass.cpp:703,920` + `scripts/direction_view.py:156` | Expected-move **INVENTADO al 2%** cuando `em=None` (legítimo y frecuente: `gex_core.py:1214` em=None sin IV) → grade LATIGAZO/REBOTE/SCALP, amplitud de flecha y gate S_APPR sobre una valla fabricada. Contradice doctrina expected-move-envelope | `em.value_or(spot*0.02)` verificado en ambas líneas; `lv.get("em") or spot*0.02` en direction_view | compass: `if (!ev.em) { a.ok=false; a.why="sin EM medido"; return a; }`; direction_view: `em = lv.get("em")` y si None el factor flip no se registra |
| 7 | `scripts/gate_core.hpp:79,326` + `scripts/gex_core.py:44,1083` (consumidores) vs `provider_bridge.py:210` (productor) | La frescura de cadena del **camino del dinero** (gate de order_engine) mide el reloj del ESCRITOR: el bridge estampa `epoch=time.time()` cada 180 s → `age` vive en 0-180 s y `stale`/`!fresh` son código muerto, mientras el `spot` interno lleva 913-1754 s | cabecera medida: `opt_chain_spy` epoch fresco pero `spot_age 913`; `opt_chain_txn` `spot_age 1754`; STALE_S=45 min es de la era IBKR | `gex_core.py:1083`: `stale = ... or (hdr.get("spot_age") or 0) > 600`; equivalente en `gate_core.hpp:326` (hdr_field ya extrae `spot_age`) |
| 8 | `scripts/compass.cpp:1462-1466` | Compass adopta el spot de `levels_<sym>.json` con gate de **mtime ≤600 s** ignorando `spot_age_s` que el productor estampa (`levels_refresh_daemon.py:178-183`) → cascada: mtime 116 s → dato de 1412-1574 s → `compass_<sym>.json` → `chart_bridge.py` `COMPASS_MAX_AGE=8` declara la flecha FRESCA sobre spot de 26 min | medido: `levels_txn.json` `spot_age_s=1412` `intrinio_quote`, mtime_age ~116 s, `stale=False` | `:1464` añadir `&& jnum(lv,"spot_age_s").value_or(1e9) <= 300` |
| 9 | `scripts/chain_cube_archive.py:266-269` + `scripts/fleet_healthcheck.py:167` | TRACE cube **muerto toda la semana sin IBKR**: `day_snapshot_paths` solo globea `opt_chain_<sym>_HHMM.txt`; esta semana solo existen `poly_chain_<sym>_HHMM.txt` (14/día, mismo formato de cabecera) → ValueError "cero fotos" en cada corrida EN SESIÓN, y el healthcheck **excusa el exit 1** como "portero horario" → falso 🟢 | `logs/trace_cube.log`: ValueError 16:10/16:20 (en ventana); `ls data/history/2026-08-04`: 14 poly_chain_spy_*.txt, 0 opt_chain_spy_HHMM; cabeceras idénticas verificadas | añadir `glob(..., "poly_chain_%s_[0-9][0-9][0-9][0-9].txt" % sl)` al set de `loose`; y que trace_cube salga con exit 2 en crash para no colisionar con el portero |
| 10 | `scripts/opt_whale_watch.py` (+`opt_whale_keepalive.sh`) y `scripts/fleet_keepalive_start.sh:227-229` (watchlist_quotes) | **Crash-loop 4001 toda la semana sin IBKR**: la Espada de Napoleón (ballenas TWS) muerta y relanzada en bucle; watchlist_quotes igual. Ninguno respeta `data/market_source.txt` (sox_feed sí: "EN PAUSA: market_source=intrinio" — ese es el patrón) | medido hoy: opt_whale 129 relanzamientos; watchlist_quotes 208 relanzamientos, 555 `Errno 61` 4001; band_open_watch 83 respawns post-ventana | al inicio de ambos: `[[ $(cat data/market_source.txt) == ibkr ]] || { echo "EN PAUSA: sin IBKR"; exit 0; }` (mismo portero que sox_feed) |

## Hallazgos medios (todos verificados fichero:línea)

**Lógica / cero plausible**
- `scripts/index_breadth.py:78-79,87,90` — `except → (0.0,"s/d",0.0)` + `continue` saca el PESO del componente del denominador del engranaje QQQ/SPY (yfinance = la fuente que ya falló en el incidente MANADA). Sin gate de cobertura mínima: con 1 componente vivo, `score=±1.00` y "ENGRANAJE BAJISTA" en el plan diario (`daily_fleet_plans.py:919-923`). Fix: gate `tot >= 0.90*sum(w)` o verdict "SIN LECTURA".
- `scripts/peer_structure.py:76-83` — mismo patrón: `comp=num/den` sin usar `n_comp` como gate; 1 componente → STRONG → `peer_coef` 1.25/0.75 escala la flecha (`direction_view.py:262-270`). Fix: `comp = num/den if (den and n_comp >= max(3, int(0.6*len(W)))) else None`.
- `scripts/signal_conditioning.py:124-131` — línea corrupta del ledger → `except: age=0` = "ocurrió AHORA", recency 1.0 (peso máximo) en `captain_flow_bias` (peso 1.2 en la flecha + multiplicador de `conditioned_prob`). Fix: `except: continue`.
- `scripts/calibration_ledger.py:149-153` — fecha ilegible → `age=0` → peso de decaimiento máximo en el `rate` publicado de `data/calibration.json`. Fix: `except: continue`.
- `scripts/bollinger_alarm.py:142-144` — veto BB **fail-open**: `except Exception: return "", True` concede permiso de voz cuando el contexto no se pudo evaluar. Fix: `return " [contexto NO EVALUABLE]", False`.
- `scripts/gex_core.py:904` (`RTH_LO <= hm <= RTH_HI`) — incluye el minuto 16:00; el resto del repo usa borde exclusivo (`bollinger_complements.py:116`, `compass.cpp:1364`, y el propio `gex_core.py:894`). Impacto acotado (bid>0 filtra), contamina `health["quotes_ok"]`. Fix: `< RTH_HI`.
- `scripts/qqq_breadth.py:29` — `SESSION_START=1784282400` (17-jul, hace 18 días) hardcodeado + denominador fabricable (`:53,58-60`). **Sin consumidor hoy** (grep verificado) — mina desarmada, pero su docstring promete cableo al fleet-check. Fix: derivar de `time.localtime()`.

**Calibración**
- Umbral de muestra **inconsistente en 4 sitios**: flow_pulse canta tasa medida con **n≥5** (`flow_pulse_calibrate.py:11-12`; medido: `CAPITAN_REVIERTE|baja` n=8 → cantaría 88%), ledger `MIN_N=20` (publica `trust:true` con n=27), compass `CALIB_MIN_N=30` (`compass.cpp:92`), barrier 50. La doctrina measured-probability es una: n≥30 o etiqueta doctrina. Fix: unificar en 30 (flow_pulse: heredar el agregado del tipo hasta n≥30).
- **Capa de condicionamiento rancia sin regenerador**: `data/signal_enable.json` (25-jul), `timeofday_factors.json` (25-jul), `session_regime.json` (25-jul), `wall_decay.json` (26-jul) — `signal_conditioning.py:204,266` los aplica EN VIVO; ningún plist/cron/weekly_autoimprove los recalcula (grep verificado). También `bollinger_probs.json`/`dip_probs.json` 22-jul, `yoel_probs.json` 23-jul. Fix: añadirlos a `weekly_autoimprove.sh`.
- `data/force.json` **14,6 días** — `force_meter.py --loop` no corre y NADA lo lanza (0 hits en *.sh/*.plist); `compass.cpp:93` degrada en silencio (gate 120 s) pero `truth_lock.py:455-460` lo estampa **sin gate de frescura** en el registro de verdad. `data/cor_fleet.json` **10 días** → `captain_damper` neutro perpetuo (degrada seguro pero nadie lo declara). Fix: relanzar force_meter en el keepalive + añadir ambos a la tabla de frescura del healthcheck.
- `scripts/weekly_autoimprove.sh:19-20` — **ciego a fallos**: pytest "2 failed" ignorado (solo `tail -1`, sin nombres) y `cpp[]` siempre vacío porque `run.sh` emite "✓ ... OK", no `PASS:|FAIL:` (verificado ejecutándolo). Fix: guardar la salida completa de pytest y grep del OK real del runner cpp.
- `scripts/daily_fleet_plans.py:263-272` (`whale_read`) — filtra por HORA del epoch pero no por FECHA: `data/whale_mu.txt` (1,36 MB, 31-jul) entró entero en el plan de hoy como flujo "de hoy". Fix: exigir `lt.tm_yday == hoy`.

**Realtime / frescura (además del TOP)**
- `scripts/compass.cpp:94` `VIX_MAX_AGE=90` vs productor a 300 s en afterhours / 600 s noche (`levels_refresh_daemon.py:65-71`); medido `vix.json` age=182 s → `vix_context:null`. En RTH 60 s de cadencia vs 90 s de gate = sin margen. Fix: 360.
- Barras: lag real medido bar-epoch→fichero: **~78 s mediana en RTH, ~15,7 min fuera de RTH** (Intrinio delayed tier). Consumidores por mtime (`fleet_pulse.py:17`, `force_meter.py:31`, `index_breadth.py:33`) creen ver "ahora" con contenido de 16 min. Fix: publicar `feed_tier` en `provider_status` y/o comparar contra el epoch de la última barra.
- `scripts/korea_watch.cpp:101` y `korea_tape.cpp:35` exigen `nbbo_kodex200/skhynix/samsung ≤120 s` — el puente Naver **declara que no escribe NBBO** (`data/korea_source.json: "nbbo": null`); medido 4,0-4,1 días; además falta `data/korea_levels.txt` → korea_watch se niega a arrancar. Cara al domingo asiático. Fix: gate por edad de barra Naver, NBBO opcional.
- `scalper/scalper_core.h:51` `NBBO_STALE_S=5` — inalcanzable con el NBBO delayed (#1): toda entrada skip y toda posición saldría FORCED. **Mitigado**: `scalper/HALT` presente. Arreglar #1 antes de rearmar.
- `scripts/intrinio_ws_probe.py` — proceso COLGADO **13,5 h** en una corrida (pid 33217, StartInterval 600 → ~80 sondas perdidas; último log 03:44). El propio healthcheck lo cantó. Fix: `signal.alarm(120)` al entrar en `main()`.

**Arquitectura**
- `scripts/ta_view.py:18` — `~/Documents/GitHub/TradingAgents` hardcodeado: bajo launchd es TCC (la regla que mató 11 jobs). Fix: env obligatorio o skip declarado.
- `scripts/compass.cpp:1507` — `book_quality.json` se consume sin gate de edad (medido: 28-jul, 7 días). Fix: mismo gate de 600 s que levels.
- Plists **divergentes** repo vs `~/Library/LaunchAgents` (verificado `cmp` en com.ibtrader.fleet → DIVERGE). Fix: script de sync + diff en healthcheck.
- `logs/truth_lock.log` 7 MB (155k líneas "SUCIO ... 0 materiales" a StartInterval 120 s) y `prints_archiver.log` 8,5 MB — ruido que entierra errores reales. Fix: log solo cuando materiales>0.
- `scripts/futures_feed.py` — martillea Databento GLBX sin licencia en CADA ciclo (~75 s) con el mismo error 4xx y cae a yfinance (delayed) para la fila viva. Fix: cachear el fallo de licencia por sesión.

## Descartado tras verificar (parecía bug y NO lo es)

| Sitio | Por qué no es bug |
|---|---|
| `fleet_consensus.py:87-95,109-123` y `fleet_consensus.cpp:351-378` | El fix de MANADA está intacto en AMBOS gemelos: mudos nombrados en `skipped`, gate de cobertura `need=0.90*flota`, pct sobre la flota COMPLETA. Hoy 17:24: "23/26 = 88%" con 3 mudos en el denominador — correcto. |
| `data/compass_calib.json` con buckets n=1-12 | `compass.cpp:92,678,852` gatea con `CALIB_MIN_N=30`: los buckets chicos NO se publican como "medido"; cae a doctrina etiquetada con tope 78. Diseño correcto. |
| ~40 usos de `time.localtime()` en ventanas horarias | El Mac ES America/New_York (`/etc/localtime`); ley de la casa declarada en `chart_levels.py:105`. Sin mezcla de zonas (la excepción real es el #2 del TOP, que sí mezcla `% 86400` UTC). |
| `cor_fleet.py` código | Ejemplar: `rho=None`+reason, gate sobre la UNIÓN, `zoneinfo` sin offsets a mano; los `1.0` del damper son neutro multiplicativo. (Lo roto es que nadie lo CORRE — ver medios.) |
| `force_meter.py:52` `return 50.0` | compass lee solo `phase`/`exhaustion` con gate 120 s; el 50 nunca llega a una decisión. |
| `compass.cpp:1348` `return 0.5` | 0.5 no cruza ningún umbral de familia (PCTB_LO/HI, band-walk); muere antes de decidir. Ídem `_pb` de fleet_consensus (diagnóstico no consumido). |
| `chart_bridge.py:1769-1775,2244-2246` defaults 0.0 | Validación de UI: cada default cae en `errors.append`/`ok:False` que RECHAZA la orden. |
| `bollinger_complements.py:290` `mins > 930` | NO es el bug histórico del 930: aquí 930=15:30 a propósito (outcome completo de 30 barras); el RTH real usa 960 en `:116`. |
| `opt_whale_watch` `UW_MAX_AGE_S=600` vs `UW_POLL_S=900` | Parece imposible; medido sobre 2.486 ticks reales: edad p75=48 s, >600 s = 0%. La API devuelve siempre ticks recientes. |
| `data/breadth.json` 4,7 h vs umbral 3 h | `dailyplans_run.sh:12` lo regenera inmediatamente antes de consumirlo. |
| `rt_last_*.txt` 2.356-5.460 s | WS Finnhub sin prints after-hours en nombres finos; los que cotizan siguen frescos (SPCX 2 s, NVDA 10 s). Normal por cierre. |
| `bars_kodex200/samsung/skhynix` mtime 02:30 ET | KRX cerró 15:30 KST = 02:30 ET. Correcto; `korea_bar_bridge.py:361-366` solo grita con KRX abierto. |
| `daily_fleet_plans.py:184` umbral 45 min sobre `opt_chain_*` | Holgado pero se cumple (bridge reescribe cada 180 s; medido age=21-32 s). El reloj equivocado que mide es el #7 del TOP. |
| `optgate` "falta el binario ./gate" (31-jul) | Ya resuelto: `optgate.py:40` prefiere `bin/gate`, que existe y es ejecutable. Log rancio. |
| `provider_syms.txt` = 26 (sin DRAM/EWY/SKHY/SPCX) | Adrede esta semana sin IBKR. No es bug. |
| `polychains.intraday` exit 1 | Su portero horario real; hoy archivó 35/35, 56.558 contratos, "sin fallos". (El falso 🟢 es el de tracecube, #9.) |
| `capitulacion_qqq.py:79-82` `return []` | Miss honesto con motivo por stderr; no fabrica dirección. |
| `confluence_engine.py`, `flow_pulse_calibrate.py` returns vacíos | Herramientas de estudio/lote nocturno: exentas por la regla. |

## Nota de orden de reparación
1→2→3→4 (dinero directo: confirmaciones bloqueadas, veto BB falso, flecha diluida, calibración
congelada) → 5→7→8 (frescura del camino del dinero) → 9→10 (visibilidad/ruido) → medios.
El #1 y el #5-productor comparten causa (provider layer sin contrato de relojes): un solo commit
en `provider_bridge.py` (dos relojes por línea + `feed_tier`) desbloquea bots, dip_alert,
price_alarm, scalper y da a todos los consumidores el dato para degradar honesto.
